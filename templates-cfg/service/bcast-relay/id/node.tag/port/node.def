type: u32

help: Destination or source port to listen and retransmit on

syntax:expression: $VAR(@) > 0 && $VAR(@) <= 65535 ; \
	"Port number must be in range 1 to 65535"

val_help: u32:1-65535; Numbered port
comp_help: Make sure that the port # matches the broadcast port.
